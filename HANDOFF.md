# Notes

Prévision PAX mensuel, 6 aéroports, Eurostat. LightGBM global vs SARIMA. Éval récursive honnête (`assume_future_exog`).

Params Optuna dans `reports/best_params.json`, chargés par `train_lightgbm_global`. Drift : `scripts/auto_retrain.py`.

Le README est la source pour les chiffres. Ne pas revendre ça comme un outil VINCI / Smart Data Hub — c’est un repo perso sur données publiques.
