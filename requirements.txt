# Runtime dependencies for the Streamlit Cloud deployment
# (dashboard + honest recursive forecast). The package is imported from src/
# via a sys.path insert in dashboard.py — no package install needed.
streamlit>=1.30
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
lightgbm>=4.0
matplotlib>=3.7
pyarrow>=14.0
